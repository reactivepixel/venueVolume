# Shared Packages

Reusable libraries belong here. Expected domains may include show models, fixture definitions, DMX frame generation, protocol adapters, timing, and UI components.

Avoid creating a shared package until at least one real consumer exists.

